# Reminder Observable / js

filter an array
iris.features
  .map((d) => d.properties)
  .filter((d) => d.TYPE == "Associative structure")

filter a geojson with bertin.js
bertin.subgeo({
  geojson: iris,
  field: "TYPE",
  operator: "==",
  array: "Associative structure"
})